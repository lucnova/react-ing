import React from 'react';
import PropTypes from 'prop-types';

const GifhoApp = (props) => {
	return (
		<>
			<h2>Gifho!</h2>
			<hr />
		</>
	);
};

GifhoApp.propTypes = {};

export default GifhoApp;
